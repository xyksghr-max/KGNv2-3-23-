# 云服务器现状

## 0. 2026-04-24 当前运行状态

2026-04-24 晚间补充：

- `T3.5b inference-side enhancement` 已在本地完成 frozen-checkpoint 评估验证。
- T3.5b 不需要 b1/e5 训练；云端只有在本地出现强正向结果或本地/云端指标不一致时才需要复测。
- 当前 T3.5b 结论是：top-k / quality filter 不进入主线，`--reproj_error_th 5` 只作为稳定质量清理策略。
- 因此云端下一次优先任务不是继续 T3.5b sweep，而应等待新分支确定后再 pull。

当前云端最近完成的主实验不是 `T3.1`，也不是 `d4ff8ca no-conf base`。
当前云端最近完成的是 `T3.5a nearest_conf` 的完整验证闭环：

```text
云端路径：/root/autodl-tmp/KGN-main
最近完成分支：feat/t3.5-conf-aware-target-selection
最近完成提交：06401fa feat: add confidence-aware pose target selection
```

最近完成的 T3.5a exp_id：

```text
smoke:
  t35_nearest_cost_smoke_b1_single_r512_e1_noval_p20_g4
  t35_nearest_conf_smoke_b1_single_r512_e1_noval_p20_g4

train:
  t35_nearest_conf_b1_single_r512_e5_val1_p20_g4

test:
  t35_nearest_conf_best_test_a07_g03_d02_a30
  t35_nearest_conf_last_test_a07_g03_d02_a30
```

最近完成结果：

```text
t35_nearest_conf_best : 0.1960 / 0.2026 / 0.7080
t35_nearest_conf_last : 0.0978 / 0.0980 / 0.4750
```

当前解释：

- `T3.5a nearest_conf` 已实现、已 smoke、已正式训练、已 best/last 评估、已回传本地分析。
- 该结果不进入主线。
- 下一步主线建议改为：从 `feat/t3.4-multigrasp-target-matching @ 1fb0084` 开 `T3.5b inference-side enhancement`。
- 本地当前已经创建 `feat/t3.5b-inference-side-enhancement` 分支；云端下一次主线 pull 应以该分支为目标。

AutoDL 策略保持不变：

- 长训练在 `screen` 中 detached 运行，关闭本地电脑或 VSCode 不会中断。
- 可以使用 AutoDL 控制台定时关机，但时间必须晚于训练/测试/文件写入完成。
- 不建议在训练未确认完成时执行 `shutdown -h now`。

说明：

- 下方旧章节保留为历史环境记录。
- 新对话如果只需要当前状态，以本节为准。

## 1. 当前总览

截至当前记录，`KGN-main` 的 AutoDL 云服务器环境已经完成核心配置，并且已经通过 single 数据集的训练、验证、测试最小闭环。当前状态可概括为：云端环境已经达到“可训练、可测试”的阶段性目标。

2026-04-19 起，项目主工作流调整为：

```text
本地 /home/xyk/KGN-main
  -> 代码修改、代码审计、文档更新、轻量静态检查、commit、push

GitHub
  -> 版本中转、分支存档、回滚锚点

云服务器 /root/autodl-tmp/KGN-main
  -> pull 最新分支、GPU smoke train、正式训练、测试评估、保存日志和实验结果

云端单实验目录打包
  -> FileZilla 下载 tar.gz
  -> 本地解压到 exp/grasp_pose
  -> 本地 Codex 读取结果继续分析
```

因此，云服务器当前主要定位从“远程 Codex 直接主开发环境”调整为“训练/评估执行端”。远程 Codex 配置仍然保留，可作为备用环境检查和日志分析工具，但不再作为主代码修改入口。

已完成：

- VSCode Remote-SSH 连接
- SSH key 免密登录 Host `autodl-kgn`
- 本地 Clash 通过 SSH RemoteForward 桥接到云端
- 远程 VSCode / Codex 代理配置
- 本地 `.codex` 授权和历史迁移到云端 `/root/.codex`
- 远程 Codex 可打开并读取云端工作区上下文
- 远程 Codex 沙箱不可用原因诊断
- 云端项目 clone
- 切换到已推送功能分支 `feat/t2-geometry-aware-confidence`
- conda 环境创建到数据盘
- PyTorch 1.13.1 pip 安装
- OpenCV headless 切换
- NumPy 版本固定
- 系统图形库补齐
- Python 依赖基础导入检查
- NMS 编译与导入检查
- DCNv2 编译与 CUDA 前向检查
- KGN DLA+DCNv2 模型 RGBD 前向检查
- 预训练权重 `ctdet_coco_dla_2x.pth` 已通过 FileZilla 上传
- 云端生成数据异常审计
- 本地 single 数据集 tar 包上传、解压与抽样图检查
- single dataset loader 检查
- T2 single 1 epoch smoke train
- T2 single test.py smoke test
- 训练日志、测试日志、`model_best.pth`、`model_last.pth` 检查
- 本地 `feat/t3-prob-pose-loss-clean` 已提交并推送到 GitHub
- 云服务器已 `fetch` 并 `checkout` 到 `feat/t3-prob-pose-loss-clean`
- 云端已确认 `T3.1` 关键文件存在
- 本地 `KGN-Pro-main` 已整体压缩上传到云端并解压为参考目录
- 云端 `KGN-Pro-main/` 已确认被主仓 `.gitignore` 忽略

未完成：

- multi 数据集最终准备
- 正式长训练
- T3.1 概率姿态辅助损失验证
- 云端单实验目录打包回传到本地后的 T3.1 结果分析

## 2. 服务器与路径状态

AutoDL 实例配置：

```text
GPU：NVIDIA GeForce RTX 4090，24GB，1 卡
CPU：16 核
内存：120GB
系统：Ubuntu 20.04
驱动：580.105.08
nvidia-smi CUDA Version：13.0
nvcc：CUDA 11.8，V11.8.89
数据盘：/root/autodl-tmp，50GB
```

重要解释：

- `nvidia-smi` 的 CUDA 13.0 只是驱动支持能力。
- 实际用于 DCNv2 编译的是系统 `nvcc 11.8`。
- PyTorch wheel 是 `cu117`，运行正常。

项目路径：

```text
/root/autodl-tmp/KGN-main
```

禁止作为云端主路径：

```text
/home/xyk/KGN-main
```

缓存与环境路径：

```text
conda pkgs: /root/autodl-tmp/conda/pkgs
conda envs: /root/autodl-tmp/conda/envs
pip cache: /root/autodl-tmp/pip-cache
tmp: /root/autodl-tmp/tmp
```

数据盘使用情况记录：

```text
安装环境后约用 6.7G
可用约 44G
single 训练/测试闭环通过并清理重复数据后：已用约 14G，可用约 37G
```

说明：

- 50GB 已足够完成当前 single 数据训练/测试最小闭环。
- 后续如果同时保留 single、multi、三组模型 checkpoint 与日志，仍建议扩容数据盘。

## 3. 代码分支状态

仓库：

```text
origin: https://github.com/xyksghr-max/KGNv2-3-23-.git
```

当前云端分支：

```text
diag-t2-d4ff8ca-base-ablation @ d4ff8ca
```

当前状态：

```text
当前正在运行 d4ff8ca no-conf base 归因实验。
不要在新会话中误以为云端仍停留在 T3.1 分支。
```

分支说明：

- `main` 分支不包含 `conf_branch`
- `feat/t2-geometry-aware-confidence` 包含 T2 几何感知置信度分支
- `feat/t3-prob-pose-loss-clean` 包含 `T3.1` 概率姿态辅助损失原型代码
- `diag-t2-d4ff8ca-base-ablation` 是当前云端归因运行分支，从 `d4ff8ca` 切出

已确认 T2 代码存在：

```text
src/lib/opts.py 中存在 --conf_branch
src/lib/trains/grasp_pose.py 中存在 conf_branch 训练逻辑
```

已确认 T3.1 关键文件存在：

```text
src/lib/models/prob_pose_aux_loss.py
src/lib/models/monte_carlo_pose_loss.py
src/lib/third_party/epropnp/pnp/
```

结论：

- 当前云端正在用于 `d4ff8ca no-conf base` 归因。
- 当前环境对后续原始 KGNv2、T2、T3.1 共用。
- `feat/t3-prob-pose-loss-clean` 仍是 T3.1 代码基线分支，但当前云端运行状态不是该分支。
- 现在不要在云端直接做新算法修改；先回收 no-conf base 结果。

## 4. Python 环境状态

当前环境：

```text
conda env: kgnv2
path: /root/autodl-tmp/conda/envs/kgnv2
Python: 3.8.20
pip: 24.2
```

激活方式：

```bash
source /root/miniconda3/etc/profile.d/conda.sh
conda activate kgnv2
```

核心包状态：

```text
torch: 1.13.1+cu117
torchvision: 0.14.1+cu117
torchaudio: 0.13.1+cu117
torch cuda runtime: 11.7
numpy: 1.23.5
opencv-contrib-python-headless: 4.5.5.64
protobuf: 3.20.3
Cython: <3
```

GPU 验证：

```text
torch.cuda.is_available(): True
torch.cuda.device_count(): 1
torch.cuda.get_device_name(0): NVIDIA GeForce RTX 4090
CUDA tensor: ok
```

已避免的问题：

- 没有安装 `pyro-ppl`
- torch 没有被升级到 2.x
- 没有安装 GUI OpenCV
- 没有让 mayavi 阻塞主线
- 没有使用 conda CUDA 工具链编译 DCNv2

## 5. 已编译组件状态

### NMS

路径：

```text
/root/autodl-tmp/KGN-main/src/lib/external
```

状态：

```text
make 编译成功
external.nms.soft_nms_39 import 成功
```

验证结果：

```text
external nms import ok: <built-in function soft_nms_39>
```

### DCNv2

路径：

```text
/root/autodl-tmp/KGN-main/src/lib/models/networks/DCNv2
```

来源：

```text
https://github.com/lucasjinreal/DCNv2_latest.git
```

编译环境：

```text
CUDA_HOME=/usr/local/cuda-11.8
FORCE_CUDA=1
TORCH_CUDA_ARCH_LIST=8.6+PTX
MAX_JOBS=4
```

状态：

```text
DCNv2 安装成功
dcn_v2 import 成功
DCN CUDA forward 成功
```

验证结果：

```text
dcn output shape: (1, 8, 32, 32)
DCNv2 cuda forward ok
```

说明：

- RTX 4090 没有使用 `8.9`
- 使用 `8.6+PTX` 编译成功
- 使用系统 CUDA 11.8 编译成功
- PyTorch cu117 与 nvcc 11.8 组合在当前机器上可用

## 6. KGN 模型检查状态

已执行项目级模型前向检查：

```text
task: grasp_pose
input_mod: RGBD
arch: dla_34
pnp_type: cvIPPE
kpt_type: box
sep_scale_branch: enabled
conf_branch: enabled
no_nms: enabled
no_kpts_refine: enabled
ori_num: 9
scale_kpts_mode: 1
```

模型输出 heads：

```text
hm: 9
w: 9
kpts_center_offset: 72
reg: 2
scales: 9
conf: 9
```

验证结果：

```text
model type: DLASeg
output type: list
num outputs: 1
output keys: ['conf', 'hm', 'kpts_center_offset', 'reg', 'scales', 'w']
KGN model forward ok
```

结论：

- KGN-main 当前云端环境已经具备模型构建和 GPU 前向能力。
- DCNv2 已被模型实际调用。
- T2 `conf_branch` head 可构建并输出。

## 7. 数据与权重状态

目录：

```text
/root/autodl-tmp/KGN-main/data
/root/autodl-tmp/KGN-main/pretrained_weights
/root/autodl-tmp/KGN-main/exp
```

预训练权重状态：

```text
/root/autodl-tmp/KGN-main/pretrained_weights/ctdet_coco_dla_2x.pth
已上传
大小约 81.0M
```

数据集状态：

```text
ps_grasp_single_1k：已使用本地上传 tar 包解压，已通过抽样图、loader、train、test 检查
ps_grasp_multi_1k：尚未准备为可用版本；云端生成版本已判定不采用
```

传输经验：

- FileZilla/SFTP 逐个传散文件很慢。
- single 数据集本地 `.tar` 包大小约 6.4G。
- 建议优先传 `.tar` 单文件。
- 当前不建议直接使用云端生成的 multi 数据集。

本地打包命令：

```bash
cd /home/xyk/KGN-main/data
tar -cf ps_grasp_single_1k.tar ps_grasp_single_1k
tar -cf ps_grasp_multi_1k.tar ps_grasp_multi_1k
```

云端解包命令：

```bash
cd /root/autodl-tmp/KGN-main/data
tar -xf ps_grasp_single_1k.tar
tar -xf ps_grasp_multi_1k.tar
```

当前 single 已确认结果：

```text
data/ps_grasp_single_1k：6.4G
train.txt：800 行
test.txt：200 行
scene 目录计数：1001，包含根目录本身
训练样本：4000
测试样本：1000
```

当前实验输出：

```text
exp/grasp_pose/t2_smoke_single_r512_e1：308M
  model_best.pth：约 77M
  model_last.pth：约 231M
  logs_2026-04-16-04-18/

exp/grasp_pose/t2_smoke_single_test：20K
  logs_2026-04-16-04-35/
```

最新空间记录：

```text
/root/autodl-tmp：50G 总量，14G 已用，37G 可用，使用率 27%
```

参考目录状态：

```text
/root/autodl-tmp/KGN-main/KGN-Pro-main
大小约 182M
来源：本地 /home/xyk/KGN-main/KGN-Pro-main 整体压缩后通过 FileZilla 上传并解压
```

说明：

- `KGN-Pro-main/` 仅作为云端参考目录，用于查阅论文 3 相关实现片段。
- `git status --short --ignored | grep KGN-Pro-main` 已确认输出 `!! KGN-Pro-main/`。
- 因此它被 `KGN-main` 主仓 `.gitignore` 忽略，不会被提交到 `KGN-main` 的 GitHub 分支。
- 上传用的 `KGN-Pro-main-full.tar.gz` 已在云端解压后删除。

## 8. 云端生成数据集审计状态

云端直接生成数据集命令可以运行，但当前不建议使用云端生成的 multi 数据作为训练数据。

已执行过的生成命令：

```bash
cd /root/autodl-tmp/KGN-main/src
python main_data_generate.py --config_file lib/data_generation/ps_grasp_single.yaml
python main_data_generate.py --config_file lib/data_generation/ps_grasp_multi.yaml
```

生成记录：

```text
single：1000/1000，约 05:22，体积约 6.4G
multi：1000/1000，约 16:40，体积约 7.0G
```

审计发现：

- 云端生成的 multi 数据图像中出现扇形/拉丝状异常面片。
- `render_imgs` 默认 `grasp_mode=-1`，正常生成 `color_image` 时不应渲染抓取 mesh。
- 审计重渲染的 `rerender_no_grasp_cam1.png` 仍然有异常，因此问题不是正常抓取可视化混入。
- 当前更稳妥的路线是使用本地确认正常的数据集上传 tar 包。

当前结论：

- 不采用云端生成的 `ps_grasp_multi_1k`。
- 云端生成的 single 不作为当前训练依据。
- 当前 single 使用本地上传并解压的数据。
- 后续 multi 应上传本地确认正常的 `ps_grasp_multi_1k.tar`，或先修复/审计生成链路后再重新生成。

## 9. single 数据加载与训练状态

single dataset loader 已通过。

检查结果：

```text
Loaded train 800 scenes with 4000 samples
data dir: /root/autodl-tmp/KGN-main/src/lib/../../data/ps_grasp_single_1k
input (4, 512, 512) float32
hm (9, 128, 128) float32
w (300, 9) float32
kpts_center_offset (300, 72) float32
scales (300, 9) float32
dataset single load ok
```

T2 single 1 epoch smoke train 已通过。

训练命令对应实验名：

```text
t2_smoke_single_r512_e1
```

训练平均结果：

```text
train loss 15.661959
train hm_loss 9.948777
train w_loss 0.020391
train kpts_center_loss 3.865062
train reg_loss(center_offset) 0.220913
train scale_loss 0.118461
train conf_loss 2.386932
train conf_mean 1.234581
train conf_geom_proxy_mean 3.904839
train time 8.466667
```

验证平均结果：

```text
val loss 6.819994
val hm_loss 1.602844
val w_loss 0.014407
val kpts_center_loss 3.700748
val reg_loss(center_offset) 0.208248
val scale_loss 0.094195
val conf_loss 2.221300
val conf_mean 1.194143
val conf_geom_proxy_mean 3.737184
val time 4.316667
```

输出文件：

```text
exp/grasp_pose/t2_smoke_single_r512_e1/model_best.pth
exp/grasp_pose/t2_smoke_single_r512_e1/model_last.pth
exp/grasp_pose/t2_smoke_single_r512_e1/opt.txt
exp/grasp_pose/t2_smoke_single_r512_e1/logs_2026-04-16-04-18/log.txt
```

说明：

- `model_best.pth` 适合 test / eval。
- `model_last.pth` 带 optimizer，适合 resume。
- 当前指标只用于 smoke train 链路判断，不作为正式实验结论。

## 10. single 测试链路状态

T2 single test.py smoke test 已通过。

测试实验名：

```text
t2_smoke_single_test
```

测试配置重点：

```text
load_model: ../exp/grasp_pose/t2_smoke_single_r512_e1/model_best.pth
ps_data_mode: single
input_mod: RGBD
keep_res: True
conf_branch: True
conf_fusion: True
conf_fusion_alpha: 0.7
conf_fusion_min_conf: 0.3
```

关键输出：

```text
Loaded test 200 scenes with 1000 samples
loaded ../exp/grasp_pose/t2_smoke_single_r512_e1/model_best.pth, epoch 1
The average time taken: 0.026027 SPF (38.420979 FPS).
```

评估输出：

```text
The grasp success rate / grasp coverage rate / object success rate:
all         : 0.0782 / 0.0396 / 0.1970
cuboid      : 0.0295 / 0.0487 / 0.2645
cylinder    : 0.0062 / 0.0115 / 0.1243
sphere      : 0.0207 / 0.0991 / 0.3419
semi_sphere : 0.0029 / 0.0086 / 0.0700
stick       : 0.0069 / 0.0940 / 0.1867
ring        : 0.0119 / 0.0469 / 0.2452
```

说明：

- 测试指标来自 1 epoch smoke 模型，不代表正式实验结果。
- 此次未启用 `--save_analysis`、`--analysis_save_kpt_vis` 或 `--vis_results`，因此测试目录只有日志和参数文件，大小约 20K。
- test.py 的推理、PnP、评估链路已确认可运行。

## 11. SSH 与远程 Codex 状态

远程开发入口：

```text
VSCode Remote-SSH Host: autodl-kgn
云端工作区: /root/autodl-tmp/KGN-main
云端 Codex 目录: /root/.codex
本地 Codex 目录: /home/xyk/.codex
```

当前已完成：

```text
SSH key 免密登录：已完成
RemoteForward 代理：已完成
远程 VSCode 代理：已完成
.codex 授权迁移：已完成
云端项目 trust_level：已加入
远程 Codex 面板：可打开，可读取云端工作区上下文
```

本地 SSH Host 关键配置：

```sshconfig
Host autodl-kgn
    HostName connect.nmb1.seetacloud.com
    Port 46224
    User root
    IdentityFile ~/.ssh/id_ed25519_autodl_kgn
    IdentitiesOnly yes
    ServerAliveInterval 60
    ServerAliveCountMax 3
    ExitOnForwardFailure yes
    RemoteForward 127.0.0.1:17890 127.0.0.1:7890
```

说明：

- `46224` 是当前 AutoDL 实例端口，后续实例重开可能变化。
- `7890` 是本地 Clash Party mixed 代理端口。
- `17890` 是云端通过 SSH 反向转发得到的代理端口。
- 密码、私钥内容、Codex token、`auth.json` 内容均不记录在文档中。

远程网络验证结果：

```text
echo $HTTP_PROXY
http://127.0.0.1:17890

curl -I -m 10 https://chatgpt.com
HTTP/1.1 200 Connection established
HTTP/2 403
```

解释：

- `HTTP/2 403` 是 Cloudflare 对 `curl` 的 challenge，不代表代理失败。
- 该结果已经说明云端请求不再 timeout，能够通过本地 Clash 访问到 `chatgpt.com`。

Codex 授权迁移状态：

```text
/root/.codex/auth.json：root root，权限 600
/root/.codex/config.toml：root root，权限 600
```

云端 `~/.codex/config.toml` 已包含：

```toml
[projects."/root/autodl-tmp/KGN-main"]
trust_level = "trusted"
```

沙箱诊断结果：

```text
unshare -Ur true
unshare: unshare failed: Operation not permitted

cat /proc/sys/kernel/unprivileged_userns_clone
1

Seccomp: 2
Seccomp_filters: 1
```

结论：

- 远程 Codex 沙箱不可用是 AutoDL 容器 seccomp / capability 限制造成的，不是仓库、网络或 `.codex` 迁移问题。
- 云端 Codex 配置保留，但当前不再作为主开发入口；主代码修改、文档更新、提交和 push 回到本地 `/home/xyk/KGN-main`。
- 云端 Codex 如临时使用，建议只用于环境检查、训练日志查看、短命令辅助和结果定位；算法修改仍优先在本地完成后通过 GitHub 同步。
- 如果后续开启更宽松权限，仍应使用项目规则限制删除、依赖安装、长训练和 `git push`。

建议：

- 只读命令可以长期允许。
- 普通源码编辑可以允许。
- 删除文件、安装依赖、重置 git、长训练、`git push` 保留人工确认。
- 在云端执行算法修改前，建议先落地项目级 `AGENTS.md`，明确禁止删除 `data/`、`pretrained_weights/`、`exp/`。

详细记录见：

```text
docs/SSH免密和远程Codex.md
```

## 12. 当前不能做或暂不建议做的事

暂不建议：

- 直接跑正式长训练
- 直接跑 400 epoch
- 在 VSCode SSH 终端裸跑长训练
- 安装 `pyro-ppl`
- 切换到 torch 2.x
- 安装 mayavi
- 用 conda 安装 CUDA nvcc / cccl 混编 DCNv2
- 在 4090 上为 torch 1.13.1 设置 `TORCH_CUDA_ARCH_LIST=8.9`
- 把 `KGN-Pro-main` 依赖整体搬入当前环境
- 使用云端生成的 `ps_grasp_multi_1k`
- 使用未经抽样审计的云端生成数据
- 直接把本地整个 `KGN-main` 拖进云端覆盖当前仓库
- 在没有项目规则和 diff 审查的情况下给远程 Codex 执行删除、安装、长训练和推送

## 13. 下一步建议

推荐顺序已更新：

1. 检查 `screen -ls`，确认 `37818.d4ff8ca_base (Detached)` 是否仍在。
2. 必要时 `screen -r d4ff8ca_base` 查看训练是否完成。
3. 检查 `exp/grasp_pose/diag_t2_d4ff8ca_base_no_conf_b1_single_r512_e5_val1_p20` 是否已有 `model_best.pth`、`model_last.pth` 和完整日志。
4. 若训练已完成但测试未跑，补跑 `diag_t2_d4ff8ca_base_no_conf_b1_best_test_d02_a30`。
5. 云端训练/评估结束后，只压缩本次 `exp/grasp_pose/<exp_id>` 单实验目录。
6. 通过 FileZilla 下载压缩包并在本地解压到 `exp/grasp_pose/`，再由本地 Codex 分析日志、指标和可视化文件。
7. 根据 no-conf base 结果，决定是否做 `paper2-clean + T2-only` 严格迁移消融。

训练前最终检查命令：

```bash
cd /root/autodl-tmp/KGN-main

git status --short
git branch --show-current

test -f pretrained_weights/ctdet_coco_dla_2x.pth && echo "pretrained ok"
test -d data/ps_grasp_single_1k && echo "single data ok"

du -sh data pretrained_weights exp
df -h /root/autodl-tmp
```

如果要长时间训练，优先使用 `screen`。当前 AutoDL 镜像未必安装 `tmux`：

```bash
screen -S d4ff8ca_base
```

detach：

```text
Ctrl-a 然后按 d
```

恢复训练窗口：

```bash
screen -r d4ff8ca_base
```
